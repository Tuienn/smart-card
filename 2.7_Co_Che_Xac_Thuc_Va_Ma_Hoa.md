# 2.7 CƠ CHẾ XÁC THỰC VÀ MÃ HÓA

## 2.7.1 Tổng quan

Bảo mật là yếu tố then chốt trong thiết kế hệ thống thẻ thông minh Entertainment. Để đảm bảo an toàn cho dữ liệu người dùng và ngăn chặn các cuộc tấn công tiềm ẩn, chúng tôi đã xây dựng một kiến trúc bảo mật nhiều lớp dựa trên các tiêu chuẩn mã hóa quốc tế như ISO/IEC 7816, FIPS 197 và RFC 2898. Hệ thống được thiết kế với triết lý "defense in depth" - phòng thủ theo chiều sâu, trong đó mỗi lớp bảo mật độc lập nhưng bổ trợ lẫn nhau, tạo thành một hệ thống bảo vệ toàn diện.

### 2.7.1.1 Kiến trúc bảo mật phân tầng

Kiến trúc bảo mật của hệ thống được tổ chức thành bốn lớp chính, mỗi lớp đảm nhận một vai trò cụ thể trong việc bảo vệ dữ liệu:

**Lớp 1 - Xác thực người dùng**: Đây là tuyến phòng thủ đầu tiên, sử dụng cơ chế PIN (Personal Identification Number) để xác minh danh tính người dùng. Lớp này triển khai hai cấp độ xác thực riêng biệt: PIN người dùng cho các thao tác thường xuyên và PIN quản trị viên cho các tác vụ quản trị. Để chống lại các cuộc tấn công brute-force, hệ thống tích hợp bộ đếm số lần thử sai và cơ chế khóa tự động.

**Lớp 2 - Quản lý khóa mã hóa**: Lớp này chịu trách nhiệm sinh, lưu trữ và quản lý vòng đời của các khóa mã hóa. Điểm đặc biệt của thiết kế là việc sử dụng mô hình Master Key, trong đó một khóa chủ duy nhất được sinh ngẫu nhiên khi khởi tạo thẻ và được bảo vệ thông qua cơ chế Key Wrapping. Master key này sau đó được sử dụng để mã hóa tất cả dữ liệu người dùng, tạo nên sự nhất quán và bảo mật cao.

**Lớp 3 - Mã hóa dữ liệu**: Tất cả dữ liệu nhạy cảm trên thẻ đều được mã hóa bằng thuật toán AES-128 ở chế độ CBC (Cipher Block Chaining). Lớp này xử lý hai loại dữ liệu chính: dữ liệu người dùng (tên, tuổi, số xu, danh sách game) được tổ chức theo chuẩn TLV, và dữ liệu hình ảnh có thể lên đến 32KB được mã hóa theo cơ chế streaming để tối ưu bộ nhớ.

**Lớp 4 - Ký số và xác thực tính toàn vẹn**: Lớp cuối cùng sử dụng mã hóa bất đối xứng RSA-1024 để tạo chữ ký số, đảm bảo tính toàn vẹn của dữ liệu và khả năng xác thực thẻ thật. Cơ chế này cho phép terminal xác minh rằng thẻ đang giao tiếp là thẻ chính hãng, không phải thẻ giả mạo.

```
┌─────────────────────────────────────────────────────────────────┐
│                    KIẾN TRÚC BẢO MẬT 4 LỚP                      │
└─────────────────────────────────────────────────────────────────┘
                              ▲
                              │
┌─────────────────────────────┴───────────────────────────────────┐
│  Lớp 4: Chữ ký số RSA                                           │
│  - RSA-1024 signature                                            │
│  - Xác thực tính toàn vẹn                                       │
└─────────────────────────────┬───────────────────────────────────┘
                              ▲
┌─────────────────────────────┴───────────────────────────────────┐
│  Lớp 3: Mã hóa dữ liệu                                          │
│  - AES-128-CBC cho user data                                     │
│  - Streaming encryption cho image                                │
└─────────────────────────────┬───────────────────────────────────┘
                              ▲
┌─────────────────────────────┴───────────────────────────────────┐
│  Lớp 2: Quản lý khóa                                            │
│  - Master Key (ngẫu nhiên)                                       │
│  - PBKDF2 Key Derivation                                         │
│  - Key Wrapping                                                  │
└─────────────────────────────┬───────────────────────────────────┘
                              ▲
┌─────────────────────────────┴───────────────────────────────────┐
│  Lớp 1: Xác thực                                                │
│  - PIN người dùng + Admin PIN                                    │
│  - Retry counter (3 lần)                                         │
│  - Auto-lock mechanism                                           │
└──────────────────────────────────────────────────────────────────┘
```


## 2.7.2 Cơ chế xác thực người dùng

### 2.7.2.1 Thiết kế hệ thống xác thực PIN

Xác thực người dùng là bước quan trọng đầu tiên để đảm bảo chỉ người dùng hợp lệ mới có thể truy cập vào dữ liệu trên thẻ. Chúng tôi đã triển khai một hệ thống xác thực hai cấp độ dựa trên PIN, được thiết kế để cân bằng giữa tính bảo mật và tính khả dụng của hệ thống.

#### 2.7.2.1.1 Mô hình xác thực hai cấp

Hệ thống sử dụng hai loại PIN riêng biệt, mỗi loại phục vụ một mục đích cụ thể:

**PIN người dùng**: Đây là PIN chính được người dùng thiết lập khi đăng ký thẻ lần đầu. PIN này có độ dài từ 4 đến 16 ký tự, cho phép người dùng linh hoạt trong việc chọn mức độ phức tạp phù hợp. PIN người dùng được yêu cầu cho tất cả các thao tác thường xuyên như xem thông tin cá nhân, chơi game, nạp xu, hoặc mua combo. Việc tách biệt PIN người dùng khỏi dữ liệu mã hóa thực tế (thông qua cơ chế Master Key) cho phép người dùng thay đổi PIN mà không cần mã hóa lại toàn bộ dữ liệu trên thẻ.

**PIN quản trị viên**: Đây là PIN đặc biệt chỉ được sử dụng cho các thao tác quản trị cấp cao như mở khóa thẻ khi người dùng nhập sai PIN quá số lần cho phép, hoặc reset toàn bộ thẻ về trạng thái ban đầu. PIN quản trị viên mặc định được thiết lập là "1234567890123456" (16 ký tự) khi khởi tạo thẻ, và có cơ chế bảo vệ riêng biệt với bộ đếm và cờ khóa độc lập. Việc tách biệt hai cấp độ xác thực này đảm bảo rằng ngay cả khi PIN người dùng bị khóa, vẫn có cách để khôi phục quyền truy cập thông qua kênh quản trị.

```
┌─────────────────────────────────────────────────────────────┐
│                   MÔ HÌNH XÁC THỰC 2 CẤP                     │
└─────────────────────────────────────────────────────────────┘

        User PIN (4-16 chars)          Admin PIN (16 chars)
               │                              │
               ▼                              ▼
        ┌─────────────┐              ┌─────────────┐
        │  PBKDF2     │              │  PBKDF2     │
        │  + Salt     │              │  + Salt     │
        └──────┬──────┘              └──────┬──────┘
               │                              │
               ▼                              ▼
          User KEK                       Admin KEK
               │                              │
               ▼                              ▼
        Unwrap Master Key            Unwrap Master Key
               │                              │
               ▼                              ▼
        ┌─────────────────────────────────────────┐
        │          Master Key (AES-128)           │
        │     - Decrypt user data                 │
        │     - Decrypt image                     │
        └─────────────────────────────────────────┘
```

#### 2.7.2.1.2 Quá trình khởi tạo và thiết lập PIN

Khi thẻ được cài đặt lần đầu tiên (thông qua lệnh `INS_INSTALL`), hệ thống thực hiện một chuỗi các thao tác quan trọng để thiết lập nền tảng bảo mật. Đầu tiên, terminal gửi PIN người dùng cùng với User ID (một định danh duy nhất 16 bytes) đến thẻ. Thẻ sẽ kiểm tra tính hợp lệ của PIN, đảm bảo độ dài nằm trong khoảng cho phép.

Sau đó, thẻ sinh ra một chuỗi salt ngẫu nhiên 16 bytes sử dụng bộ sinh số ngẫu nhiên an toàn (Secure Random). Salt này đóng vai trò quan trọng trong việc tăng cường độ mạnh của quá trình tạo khóa, đảm bảo rằng ngay cả hai thẻ có cùng PIN cũng sẽ tạo ra các khóa mã hóa khác nhau. Đồng thời, thẻ cũng sinh ra Master Key - một khóa AES-128 hoàn toàn ngẫu nhiên, độc lập với PIN người dùng. Đây là một quyết định thiết kế quan trọng vì nó cho phép tách biệt việc xác thực (thông qua PIN) với việc mã hóa dữ liệu (thông qua Master Key).

Master Key sau đó được "bọc" (wrap) bằng một Key Encryption Key (KEK) được tạo ra từ PIN người dùng và salt thông qua hàm PBKDF2. Quá trình này đảm bảo rằng Master Key không bao giờ được lưu trực tiếp trên thẻ mà chỉ tồn tại dưới dạng mã hóa. Tương tự, Admin PIN mặc định cũng được sử dụng để tạo ra một Admin KEK và wrap Master Key một lần nữa, tạo ra hai "bản sao được bảo vệ" của cùng một Master Key.

#### 2.7.2.1.3 Cơ chế chống tấn công Brute-force

Một trong những mối đe dọa phổ biến nhất đối với hệ thống xác thực PIN là tấn công brute-force, trong đó kẻ tấn công thử tất cả các kết hợp PIN có thể cho đến khi tìm ra đúng. Để chống lại loại tấn công này, chúng tôi đã triển khai ba cơ chế bảo vệ lồng ghép:

**Bộ đếm số lần thử (Try Counter)**: Hệ thống duy trì hai bộ đếm riêng biệt cho PIN người dùng và Admin PIN, mỗi bộ được khởi tạo với giá trị 3. Mỗi lần xác thực thất bại, bộ đếm tương ứng giảm đi 1. Khi bộ đếm về 0, cơ chế khóa tự động được kích hoạt. Sau mỗi lần xác thực thành công, bộ đếm được reset về giá trị ban đầu (3 lần).

**Cờ khóa (Lock Flag)**: Khi bộ đếm về 0, một cờ boolean `lockedFlag` (hoặc `adminLockedFlag` cho Admin PIN) được set thành `true`. Khi cờ này được kích hoạt, mọi lệnh yêu cầu xác thực đều bị từ chối ngay lập tức với mã lỗi `SW_AUTHENTICATION_BLOCKED` (0x6983), ngay cả khi PIN đúng được cung cấp. Điều này ngăn chặn hiệu quả việc tiếp tục thử PIN.

**Phản hồi số lần thử còn lại**: Tuân theo chuẩn ISO 7816-4, hệ thống trả về mã trạng thái đặc biệt `0x63Cx` (trong đó x là số lần thử còn lại) sau mỗi lần xác thực thất bại. Điều này cho phép terminal hoặc ứng dụng client cảnh báo người dùng về số lần thử còn lại trước khi thẻ bị khóa hoàn toàn, giúp người dùng cẩn trọng hơn trong việc nhập PIN.

### 2.7.2.2 Quy trình xác thực chi tiết

Quá trình xác thực PIN được thiết kế cẩn thận để đảm bảo an toàn tối đa cho dữ liệu nhạy cảm. Khi terminal gửi lệnh xác thực PIN (`INS_VERIFY_PIN`), hệ thống thực hiện một chuỗi các bước kiểm tra và xử lý:

**Bước 1 - Kiểm tra điều kiện tiên quyết**: Trước tiên, hệ thống kiểm tra xem thẻ đã được khởi tạo chưa (flag `initialized`) và có đang bị khóa hay không (flag `lockedFlag`). Nếu thẻ chưa khởi tạo hoặc đang bị khóa, quá trình xác thực dừng ngay lập tức và trả về mã lỗi tương ứng.

**Bước 2 - Nhận và xử lý PIN**: PIN được gửi từ terminal trong phần dữ liệu của APDU. Hệ thống copy PIN vào một mảng tạm thời để xử lý. Điểm quan trọng là PIN không được lưu trữ lâu dài - nó chỉ tồn tại trong bộ nhớ đủ lâu để thực hiện các phép tính cần thiết.

**Bước 3 - Tạo KEK thông qua PBKDF2**: Sử dụng PIN người dùng và salt đã được lưu từ lúc khởi tạo, hệ thống gọi hàm PBKDF2 với 500 vòng lặp để tạo ra KEK. PBKDF2 (Password-Based Key Derivation Function 2) là một hàm chuẩn công nghiệp được thiết kế đặc biệt để chuyển đổi password thành khóa mã hóa mạnh. Số vòng lặp 500 được chọn để cân bằng giữa bảo mật (càng nhiều vòng lặp càng khó tấn công) và hiệu năng trên JavaCard (phần cứng hạn chế).

**Bước 4 - Xóa PIN khỏi bộ nhớ**: Ngay sau khi KEK được tạo, PIN được xóa hoàn toàn khỏi bộ nhớ bằng cách ghi đè tất cả các byte bằng giá trị 0. Đây là một thực hành bảo mật quan trọng để đảm bảo PIN không bao giờ tồn tại lâu trong bộ nhớ và giảm thiểu rủi ro bị đọc trộm.

**Bước 5 - Unwrap Master Key**: Sử dụng KEK vừa tạo, hệ thống thực hiện thao tác "unwrap" (giải bọc) để lấy lại Master Key từ dạng mã hóa đã lưu trữ (`wrappedMasterKey`). Quá trình này bao gồm: trích xuất IV (Initialization Vector) 16 bytes đầu tiên từ wrappedMasterKey, khởi tạo AES cipher ở chế độ decrypt với KEK và IV, sau đó giải mã 16 bytes tiếp theo để thu được Master Key.

**Bước 6 - Xóa KEK**: Tương tự như PIN, KEK cũng được xóa ngay sau khi sử dụng xong để bảo vệ bảo mật.

**Bước 7 - Xác minh Master Key**: Đây là bước quan trọng để phát hiện PIN sai. Hệ thống hash Master Key vừa unwrap được bằng SHA-1 và so sánh với hash đã lưu từ lúc khởi tạo (`masterKeyHash`). Nếu hai hash khớp nhau, chứng tỏ PIN đúng và Master Key được unwrap chính xác. Nếu không khớp, chứng tỏ PIN sai.

**Bước 8 - Cập nhật trạng thái**: Nếu xác thực thành công, cờ `sessionAuth` được set thành `true`, cho phép các lệnh tiếp theo truy cập dữ liệu. Bộ đếm thử PIN được reset về 3. Master Key được giữ trong transient memory để sử dụng cho các thao tác mã hóa/giải mã trong phiên làm việc hiện tại. Nếu xác thực thất bại, Master Key bị xóa ngay, bộ đếm giảm đi 1, và có thể kích hoạt khóa nếu hết số lần thử.

### 2.7.2.3 Quản lý phiên xác thực

Để cân bằng giữa bảo mật và trải nghiệm người dùng, hệ thống triển khai cơ chế quản lý phiên (session management). Sau khi xác thực thành công, người dùng không cần nhập lại PIN cho mỗi thao tác tiếp theo trong cùng một phiên làm việc. Phiên làm việc được duy trì bởi cờ `sessionAuth` và Master Key được lưu trong transient memory.

Phiên làm việc tự động kết thúc trong các trường hợp sau: (1) Khi thẻ bị reset về mặt vật lý (mất nguồn điện); (2) Khi applet bị deselect (chọn applet khác); (3) Khi người dùng chủ động thực hiện lệnh logout (nếu được triển khai). Khi phiên kết thúc, Master Key trong transient memory tự động bị xóa sạch, đảm bảo không có thông tin nhạy cảm nào còn lại trong bộ nhớ.

### 2.7.2.4 Thay đổi PIN an toàn

Một tính năng quan trọng của hệ thống là khả năng thay đổi PIN mà không làm mất dữ liệu hoặc cần mã hóa lại toàn bộ thẻ. Điều này có thể thực hiện được nhờ vào kiến trúc Master Key. Quy trình thay đổi PIN bao gồm các bước sau:

Đầu tiên, hệ thống yêu cầu người dùng phải đã xác thực thành công trong phiên hiện tại (kiểm tra `sessionAuth`). Đây là biện pháp bảo vệ đầu tiên, đảm bảo chỉ người dùng hợp lệ mới có thể thay đổi PIN. Tiếp theo, người dùng phải cung cấp cả PIN cũ và PIN mới. Hệ thống thực hiện xác thực lại PIN cũ (re-verification) như một lớp bảo vệ thứ hai, ngăn chặn trường hợp thiết bị bị để không khóa và người khác lợi dụng phiên đã xác thực để đổi PIN.

Sau khi xác minh PIN cũ thành công, hệ thống tạo KEK mới từ PIN mới và salt (salt không thay đổi, vẫn giữ nguyên từ lúc khởi tạo). Master Key sau đó được wrap lại bằng KEK mới này, tạo ra `wrappedMasterKey` mới. Quan trọng là Master Key bản thân không thay đổi - chỉ có cách nó được bảo vệ là thay đổi. Điều này có nghĩa là tất cả dữ liệu đã được mã hóa trước đó vẫn có thể giải mã được bằng cùng Master Key, không cần mã hóa lại.

Cuối cùng, tất cả các dữ liệu nhạy cảm trung gian (PIN cũ, PIN mới, KEK cũ, KEK mới) đều được xóa sạch khỏi bộ nhớ. Phiên làm việc vẫn được duy trì (không cần xác thực lại), cho phép người dùng tiếp tục sử dụng thẻ ngay lập tức.

## 2.7.3 Cơ chế quản lý khóa mã hóa

### 2.7.3.1 Kiến trúc Master Key và lý do thiết kế

Một trong những quyết định thiết kế quan trọng nhất của hệ thống là việc áp dụng mô hình Master Key. Thay vì sử dụng trực tiếp PIN để mã hóa dữ liệu người dùng, chúng tôi tạo ra một lớp trung gian: Master Key. Đây là một khóa AES-128 (16 bytes) được sinh hoàn toàn ngẫu nhiên khi thẻ được khởi tạo lần đầu, độc lập với PIN người dùng.

Lợi ích chính của mô hình này là khả năng tách biệt hoàn toàn giữa xác thực (authentication) và mã hóa (encryption). Khi người dùng muốn thay đổi PIN, chúng ta chỉ cần thay đổi cách Master Key được bảo vệ (thông qua việc wrap lại với KEK mới), mà không cần phải giải mã và mã hóa lại toàn bộ dữ liệu trên thẻ. Điều này đặc biệt quan trọng khi xem xét rằng thẻ có thể chứa hàng chục KB dữ liệu hình ảnh - việc mã hóa lại sẽ tốn rất nhiều thời gian và tài nguyên.

Master Key được lưu trữ trong transient memory (bộ nhớ tạm) với thuộc tính CLEAR_ON_RESET, nghĩa là nó tự động bị xóa khi thẻ mất nguồn hoặc bị reset. Trong persistent memory (bộ nhớ vĩnh viễn), chúng ta chỉ lưu Master Key ở dạng đã được mã hóa (wrapped) bởi KEK. Có hai bản wrapped key riêng biệt: một được bảo vệ bởi User KEK (từ PIN người dùng) và một được bảo vệ bởi Admin KEK (từ Admin PIN). Cả hai đều wrap cùng một Master Key, cho phép khôi phục quyền truy cập thông qua hai con đường độc lập.

```
┌────────────────────────────────────────────────────────────────┐
│                    KIẾN TRÚC MASTER KEY                        │
└────────────────────────────────────────────────────────────────┘

    PERSISTENT STORAGE              TRANSIENT MEMORY
┌───────────────────────┐         ┌─────────────────┐
│ User Wrapped Key      │         │   Master Key    │
│ (PIN protected)       │────────>│   (AES-128)     │
│   - IV (16 bytes)     │ unwrap  │                 │
│   - Encrypted (16 B)  │         │ Used for:       │
└───────────────────────┘         │ • User data enc │
                                  │ • Image enc     │
┌───────────────────────┐         └────────┬────────┘
│ Admin Wrapped Key     │                  │
│ (Admin PIN protected) │────────>         │
│   - IV (16 bytes)     │ unwrap           │
│   - Encrypted (16 B)  │                  ▼
└───────────────────────┘         ┌─────────────────┐
                                  │  Encrypted Data │
┌───────────────────────┐         │  • User info    │
│ Master Key Hash       │         │  • Image data   │
│ (SHA-1, 20 bytes)     │         └─────────────────┘
│ For verification      │
└───────────────────────┘
```

### 2.7.3.2 Tạo khóa từ PIN: PBKDF2

Chuyển đổi PIN thành khóa mã hóa mạnh là một thách thức quan trọng trong thiết kế bảo mật. PIN thường ngắn và dễ nhớ (4-6 số), do đó có entropy thấp và dễ bị tấn công dictionary hoặc brute-force. Để giải quyết vấn đề này, chúng tôi sử dụng PBKDF2 (Password-Based Key Derivation Function 2) - một hàm chuẩn công nghiệp được định nghĩa trong RFC 2898.

PBKDF2 hoạt động bằng cách kết hợp PIN với một chuỗi salt ngẫu nhiên, sau đó áp dụng hàm hash (SHA-256) lặp đi lặp lại nhiều lần (500 iterations trong triển khai của chúng tôi). Mỗi vòng lặp làm tăng độ phức tạp tính toán, khiến việc thử các PIN khác nhau trở nên chậm hơn đáng kể đối với kẻ tấn công. Số lượng 500 iterations được chọn sau khi cân nhắc giữa bảo mật (càng nhiều càng tốt) và hiệu năng thực tế trên phần cứng JavaCard hạn chế (quá nhiều sẽ làm thời gian xác thực quá lâu).

Salt đóng vai trò quan trọng trong việc ngăn chặn tấn công rainbow table và đảm bảo rằng hai thẻ có cùng PIN vẫn tạo ra các KEK hoàn toàn khác nhau. Salt được sinh ngẫu nhiên khi khởi tạo thẻ (16 bytes) và được lưu trữ công khai trên thẻ - không cần bảo mật salt vì chức năng của nó là làm unique mỗi card, không phải giữ bí mật.

Để đảm bảo khả năng tương thích với các thẻ cũ hoặc không hỗ trợ PBKDF2, hệ thống cũng triển khai một fallback KDF đơn giản hơn: nối PIN với salt rồi hash một lần bằng SHA-1, lấy 16 bytes đầu làm KEK. Tuy kém bảo mật hơn PBKDF2 nhưng vẫn tốt hơn nhiều so với việc dùng trực tiếp PIN.

### 2.7.3.3 Bảo vệ Master Key: Key Wrapping

Master Key không bao giờ được lưu trực tiếp ở dạng plain text trên thẻ. Thay vào đó, nó được "bọc" (wrap) bằng KEK thông qua thuật toán AES-CBC. Quá trình wrapping diễn ra như sau:

Đầu tiên, hệ thống sinh một IV (Initialization Vector) ngẫu nhiên 16 bytes. IV này đảm bảo rằng ngay cả khi cùng một Master Key được wrap nhiều lần (ví dụ khi đổi PIN), kết quả mã hóa sẽ khác nhau mỗi lần. IV được lưu công khai ở 16 bytes đầu của wrapped key.

Tiếp theo, Master Key (16 bytes) được mã hóa bằng AES-CBC sử dụng KEK làm khóa và IV vừa sinh. Kết quả là 16 bytes ciphertext được lưu ngay sau IV. Tổng cộng, wrapped key chiếm 32 bytes trong persistent storage.

Cấu trúc wrapped key:
```
┌────────────────────────────┬─────────────────────────────┐
│  IV (16 bytes - public)    │  Encrypted Master Key       │
│  Random for each wrap      │  (16 bytes - secret)        │
└────────────────────────────┴─────────────────────────────┘
        Offset 0                      Offset 16
```

Quá trình unwrapping (giải bọc) là ngược lại: trích xuất IV từ 16 bytes đầu, sử dụng KEK để giải mã 16 bytes tiếp theo, thu được Master Key. Nếu KEK sai (do PIN sai), quá trình giải mã vẫn thành công về mặt kỹ thuật nhưng sẽ cho ra một chuỗi byte ngẫu nhiên thay vì Master Key đúng. Đây là lý do tại sao chúng ta cần thêm bước xác minh hash ở sau.

### 2.7.3.4 Xác minh tính đúng đắn của Master Key

Một vấn đề kỹ thuật quan trọng của AES-CBC là thuật toán này không có khả năng tự phát hiện lỗi (no authentication). Điều này có nghĩa là khi chúng ta unwrap Master Key bằng một KEK sai (từ PIN sai), quá trình giải mã vẫn hoàn thành "thành công" và trả về 16 bytes dữ liệu - nhưng đó chỉ là garbage data, không phải Master Key thật.

Nếu chúng ta không có cơ chế kiểm tra, hệ thống sẽ chấp nhận Master Key sai này và tiếp tục sử dụng nó để "giải mã" dữ liệu người dùng, dẫn đến kết quả vô nghĩa. Tệ hơn, điều này có thể bị lợi dụng trong các cuộc tấn công chosen-ciphertext, nơi kẻ tấn công cố tình cung cấp wrapped key giả mạo và quan sát phản ứng của hệ thống.

Để giải quyết vấn đề này, chúng tôi triển khai một lớp xác minh bằng hash. Khi thẻ được khởi tạo, Master Key thật được hash bằng SHA-1, tạo ra một "fingerprint" 20 bytes được lưu trong `masterKeyHash`. Mỗi khi unwrap Master Key, chúng ta hash lại kết quả thu được và so sánh với hash đã lưu. Chỉ khi hai hash khớp nhau hoàn toàn (cả 20 bytes), chúng ta mới chấp nhận Master Key là đúng.

Cơ chế này mang lại ba lợi ích quan trọng: (1) Phát hiện chính xác PIN sai, ngay cả khi quá trình unwrap không báo lỗi; (2) Ngăn chặn các cuộc tấn công chosen-ciphertext bằng cách đảm bảo hệ thống chỉ làm việc với Master Key hợp lệ; (3) Bảo vệ tính toàn vẹn của Master Key khỏi lỗi phần cứng hoặc corruption dữ liệu.

## 2.7.4 Mã hóa dữ liệu người dùng

### 2.7.4.1 Tổ chức dữ liệu theo chuẩn TLV

Dữ liệu người dùng trên thẻ bao gồm nhiều trường khác nhau với kích thước động (tên có thể dài hoặc ngắn, danh sách game mua có thể thay đổi). Để quản lý hiệu quả các trường dữ liệu có độ dài biến đổi này, chúng tôi áp dụng chuẩn TLV (Tag-Length-Value) - một format được sử dụng rộng rãi trong các hệ thống thẻ thông minh và giao thức truyền thông.

Trong cấu trúc TLV, mỗi trường dữ liệu được biểu diễn bởi ba thành phần: **Tag** (1 byte) xác định loại dữ liệu (tên, tuổi, số xu...), **Length** (1 byte) cho biết độ dài của dữ liệu, và **Value** (độ dài thay đổi) chứa dữ liệu thực tế. Format này mang lại nhiều lợi ích: cho phép thêm/xóa trường mà không ảnh hưởng cấu trúc tổng thể, tiết kiệm bộ nhớ bằng cách chỉ lưu dữ liệu cần thiết, và dễ dàng parse/serialize.

Hệ thống định nghĩa 5 tag chính:
- **TAG_NAME (0x01)**: Tên người dùng, độ dài tối đa 64 bytes
- **TAG_GENDER (0x02)**: Giới tính, 1 byte (0=không xác định, 1=nam, 2=nữ)
- **TAG_COINS (0x03)**: Số xu hiện có, 2 bytes (short, 0-65535)
- **TAG_BOUGHT_GAMES (0x04)**: Danh sách ID game đã mua, độ dài động
- **TAG_AGE (0x05)**: Tuổi người dùng, 1 byte (0-255)

Khi thẻ được khởi tạo, các trường được thiết lập với giá trị mặc định: tên rỗng (length=0), giới tính=0, coins=0, danh sách game rỗng, tuổi=0. Cấu trúc TLV sau đó được mã hóa toàn bộ thành một khối 160 bytes cố định (đã padding) trước khi lưu vào persistent storage.

**Ví dụ cấu trúc TLV** (dữ liệu đã giải mã):
```
┌────┬────┬─────────────┬────┬────┬───┬────┬────┬─────────┐
│ 01 │ 04 │ J o h n    │ 02 │ 01 │ 1 │ 03 │ 02 │ 01 F4   │
└────┴────┴─────────────┴────┴────┴───┴────┴────┴─────────┘
 Tag  Len   Value(Name)  Tag  Len  M  Tag  Len  500 coins

Giải thích:
- Tên: "John" (4 bytes)
- Giới tính: Nam (1)
- Số xu: 500 (0x01F4)
```

### 2.7.4.2 Mã hóa với AES-128-CBC

Sau khi dữ liệu người dùng được tổ chức theo chuẩn TLV, toàn bộ cấu trúc này cần được mã hóa để bảo vệ tính riêng tư. Chúng tôi sử dụng thuật toán AES-128 ở chế độ CBC (Cipher Block Chaining) - một trong những chế độ mã hóa block cipher được khuyến nghị bởi NIST.

Quá trình mã hóa diễn ra như sau: Đầu tiên, hệ thống sinh một IV (Initialization Vector) ngẫu nhiên 16 bytes cho mỗi lần mã hóa. IV đảm bảo rằng ngay cả khi dữ liệu giống nhau được mã hóa nhiều lần, ciphertext sẽ luôn khác nhau, ngăn chặn các cuộc tấn công phân tích pattern. IV được lưu công khai ở 16 bytes đầu tiên của encrypted data.

Tiếp theo, dữ liệu TLV (160 bytes đã padding) được mã hóa bằng AES-CBC sử dụng Master Key. Do AES yêu cầu dữ liệu đầu vào là bội số của 16 bytes, chúng tôi cố định kích thước plaintext là 160 bytes (10 blocks). Nếu dữ liệu TLV thực tế ngắn hơn, phần còn lại được padding bằng số 0. Kết quả mã hóa là 160 bytes ciphertext được lưu ngay sau IV.

Tổng cộng, encrypted user data chiếm 176 bytes trong persistent storage:

```
┌────────────────┬──────────────────────────────────────┐
│  IV (16 bytes) │  Encrypted TLV Data (160 bytes)      │
│   (Public)     │        (Confidential)                │
└────────────────┴──────────────────────────────────────┘
     Offset 0              Offset 16
```

Quá trình giải mã là ngược lại: trích xuất IV từ 16 bytes đầu, sau đó sử dụng Master Key và IV này để giải mã 160 bytes tiếp theo, thu được dữ liệu TLV gốc. Việc sử dụng CBC mode có ưu điểm là mỗi block ciphertext phụ thuộc vào tất cả các plaintext block trước đó, tạo hiệu ứng "lan truyền" làm tăng độ bảo mật.

### 2.7.4.3 Thao tác trên dữ liệu được mã hóa

Một thách thức kỹ thuật quan trọng là làm thế nào để đọc/ghi từng trường dữ liệu mà không cần giải mã/mã hóa lại toàn bộ. Do AES-CBC mã hóa theo block và các block phụ thuộc lẫn nhau, chúng ta không thể decrypt chỉ một phần nhỏ của ciphertext. Giải pháp của chúng tôi là:

**Đọc một trường**: Khi cần đọc một trường cụ thể (ví dụ TAG_COINS), hệ thống phải giải mã toàn bộ 160 bytes encrypted data để lấy plaintext TLV đầy đủ. Sau đó, hệ thống duyệt qua cấu trúc TLV để tìm trường có tag tương ứng, trích xuất giá trị và trả về cho terminal. Mặc dù phải giải mã toàn bộ, nhưng việc này diễn ra rất nhanh (AES-128 decrypt 160 bytes mất < 50ms trên JavaCard) nên không ảnh hưởng đáng kể đến hiệu năng.

**Ghi một trường**: Quá trình phức tạp hơn đọc vì cần đảm bảo tính toàn vẹn của cấu trúc TLV. Hệ thống thực hiện các bước: (1) Giải mã toàn bộ dữ liệu hiện tại; (2) Tìm trường cần cập nhật trong TLV; (3) Nếu độ dài value mới bằng độ dài cũ, cập nhật trực tiếp tại chỗ; (4) Nếu độ dài thay đổi, rebuild lại toàn bộ cấu trúc TLV để đảm bảo tính compact (không có gaps); (5) Mã hóa lại toàn bộ với IV mới và lưu.

Việc rebuild TLV khi độ dài thay đổi là cần thiết để tránh lãng phí bộ nhớ và đảm bảo cấu trúc dữ liệu nhất quán. Quá trình này diễn ra bằng cách tạo một TLV buffer mới, copy tất cả các trường từ TLV cũ sang, nhưng thay thế trường được cập nhật bằng giá trị mới. Cuối cùng, TLV buffer mới này ghi đè lên TLV cũ.

**Tối ưu hóa**: Để giảm số lần mã hóa/giải mã không cần thiết, khi có nhiều thao tác ghi liên tiếp, ứng dụng client có thể gộp chúng thành một APDU chứa nhiều cặp tag-value, cho phép hệ thống chỉ giải mã một lần, cập nhật tất cả các trường, rồi mã hóa lại một lần duy nhất.

## 2.7.5 Mã hóa hình ảnh

### 2.7.5.1 Thách thức và giải pháp

Hình ảnh người dùng (avatar, ảnh cá nhân) là dữ liệu có kích thước lớn, có thể lên đến 32KB - gấp hàng trăm lần so với user data cơ bản. Điều này tạo ra nhiều thách thức kỹ thuật đặc thù:

**Thách thức về bộ nhớ**: JavaCard có bộ nhớ RAM rất hạn chế (thường < 4KB), không đủ để load toàn bộ hình ảnh vào bộ nhớ cùng lúc. Việc cố gắng đọc/ghi 32KB một lần sẽ gây lỗi out-of-memory. Do đó, chúng tôi phải thiết kế cơ chế xử lý theo từng phần nhỏ (chunk).

**Thách thức về APDU**: Giao thức ISO 7816 giới hạn kích thước APDU tối đa là 255 bytes (hoặc extended APDU 65535 bytes, nhưng không phải thẻ nào cũng hỗ trợ). Do đó, việc truyền hình ảnh phải chia thành nhiều APDU liên tiếp.

**Thách thức về CBC chaining**: AES-CBC yêu cầu mỗi block phụ thuộc vào block trước đó. Nếu mã hóa từng chunk độc lập, chúng ta mất đi thuộc tính bảo mật quan trọng này. Giải pháp là duy trì trạng thái cipher xuyên suốt toàn bộ quá trình mã hóa.

Chúng tôi giải quyết các thách thức này bằng cơ chế **streaming encryption**: cipher được khởi tạo một lần khi bắt đầu ghi ảnh, sau đó duy trì trạng thái để xử lý từng chunk liên tiếp, và cuối cùng finalize khi hoàn tất. Điều này đảm bảo tính liên tục của CBC chain trong khi vẫn xử lý được dữ liệu lớn với bộ nhớ hạn chế.

### 2.7.5.2 Quy trình ghi hình ảnh theo chunk

Quá trình ghi hình ảnh được thiết kế thành hai giai đoạn riêng biệt để xử lý dữ liệu lớn một cách hiệu quả:

**Giai đoạn 1 - Khởi tạo** (lệnh `INS_WRITE_IMAGE_START`):

Terminal gửi thông tin metadata của hình ảnh: tổng kích thước (2 bytes), loại hình ảnh (1 byte: JPG, PNG...), và chunk dữ liệu đầu tiên. Thẻ thực hiện các bước quan trọng sau:

1. **Kiểm tra dung lượng**: Xác minh tổng kích thước không vượt quá giới hạn 32KB (MAX_IMAGE_SIZE). Nếu vượt quá, từ chối ngay với mã lỗi `SW_NOT_ENOUGH_MEMORY`.

2. **Sinh IV ngẫu nhiên**: Tạo một IV 16 bytes cho toàn bộ quá trình mã hóa hình ảnh. IV này được lưu riêng trong `imageIV` để sử dụng cho giải mã sau này.

3. **Khởi tạo cipher một lần**: Đây là điểm quan trọng - cipher được init ở MODE_ENCRYPT với Master Key và IV, sau đó **không bao giờ** re-init cho đến khi hoàn tất mã hóa toàn bộ ảnh. Điều này đảm bảo CBC chain được duy trì liên tục.

4. **Mã hóa chunk đầu**: Chunk dữ liệu đầu tiên (thường ~240 bytes) được mã hóa ngay. Vì AES yêu cầu dữ liệu là bội số của 16 bytes, hệ thống sử dụng buffer tạm `tempImageChunk` để tích lũy dữ liệu cho đến khi đủ 16 bytes.

5. **Lưu trạng thái**: Các biến trạng thái được cập nhật: `currentWriteOffset` (vị trí ghi tiếp theo), `tempChunkLen` (số byte dư chưa đủ 16), `totalImageSize` (để kiểm tra khi nào hoàn tất).

**Giai đoạn 2 - Tiếp tục ghi** (lệnh `INS_WRITE_IMAGE_CONTINUE`):

Sau khi chunk đầu tiên được ghi thành công, các chunk tiếp theo được gửi liên tục thông qua lệnh CONTINUE. Mỗi APDU trong giai đoạn này bao gồm: offset hình ảnh hiện tại (2 bytes để tracking) và chunk dữ liệu tiếp theo (~240 bytes).

Hệ thống xử lý mỗi chunk theo quy trình tích lũy và mã hóa thông minh:

1. **Xử lý dữ liệu dư**: Nếu chunk trước còn dư một số byte chưa đủ để tạo thành block 16 bytes (lưu trong `tempImageChunk`), hệ thống sẽ kết hợp dữ liệu dư này với dữ liệu mới. Khi đủ 16 bytes, block hoàn chỉnh được mã hóa ngay bằng phương thức `cipher.update()` - **quan trọng là không re-init cipher**, đảm bảo chuỗi CBC được duy trì liên tục từ chunk trước.

2. **Mã hóa các block đầy đủ**: Sau khi xử lý dữ liệu dư, hệ thống duyệt qua chunk mới và mã hóa tất cả các block 16 bytes đầy đủ. Mỗi block được mã hóa trực tiếp vào `imageBuffer` tại vị trí `currentWriteOffset`, sau đó offset tăng thêm 16.

3. **Lưu phần dư mới**: Nếu chunk còn lại một số byte không đủ 16 (ví dụ chunk 240 bytes = 15 blocks đầy đủ), các byte dư này được copy vào `tempImageChunk` để chờ chunk tiếp theo.

4. **Finalization khi hoàn tất**: Hệ thống theo dõi tổng số byte đã nhận. Khi phát hiện đã nhận đủ dữ liệu (thông qua so sánh `totalReceived >= totalImageSize`), quá trình kết thúc được kích hoạt. Nếu còn dữ liệu dư trong `tempImageChunk`, hệ thống áp dụng **PKCS#7 padding** - một chuẩn padding trong đó mỗi byte padding có giá trị bằng số lượng byte padding. Ví dụ: nếu còn thiếu 5 bytes để đủ 16, thì 5 bytes cuối sẽ là `0x05 0x05 0x05 0x05 0x05`. Block cuối cùng này được mã hóa bằng `cipher.doFinal()` để hoàn tất quá trình.

5. **Dọn dẹp**: Sau khi hoàn tất, tất cả buffer tạm và biến trạng thái được xóa sạch để đảm bảo bảo mật.

**Đặc điểm quan trọng của thiết kế streaming**:

- **CBC Chain Continuity**: Cipher object được khởi tạo một lần duy nhất ở giai đoạn START và giữ nguyên trạng thái nội bộ qua tất cả các lần gọi `update()`. Điều này đảm bảo mỗi block mã hóa phụ thuộc vào block trước đó, tạo nên tính liên kết bảo mật của CBC mode.

- **Memory Efficiency**: Tại bất kỳ thời điểm nào, hệ thống chỉ cần giữ trong RAM: chunk hiện tại (~240 bytes), temp buffer (16 bytes), và các biến trạng thái. Điều này cho phép xử lý hình ảnh 32KB với bộ nhớ RAM < 4KB.

- **Error Recovery**: Nếu một chunk bị lỗi giữa chừng, toàn bộ quá trình mã hóa phải bắt đầu lại từ đầu vì không thể khôi phục trạng thái cipher ở giữa chuỗi CBC.

**Sơ đồ quá trình ghi liên tục**:
```
Chunk 1 (240B) → [Cipher.update] → 15 blocks encrypted + 0 byte dư
Chunk 2 (240B) → [Cipher.update] → 15 blocks encrypted + 0 byte dư
Chunk 3 (240B) → [Cipher.update] → 15 blocks encrypted + 0 byte dư
    ...
Chunk N (120B) → [Tích lũy]    → 7 blocks encrypted + 8 byte dư
                  [PKCS#7 pad]  → Thêm 8 bytes padding (0x08)
                  [Cipher.doFinal] → 1 block cuối encrypted
                  ✓ Hoàn tất
```

### 2.7.5.3 Đọc hình ảnh theo block

Đọc hình ảnh từ thẻ phức tạp hơn ghi vì terminal thường chỉ yêu cầu một phần của hình ảnh tại một thời điểm (ví dụ: byte 1000-2000), trong khi dữ liệu được mã hóa theo block 16 bytes và các block phụ thuộc lẫn nhau trong CBC mode. Chúng tôi giải quyết vấn đề này bằng cách triển khai cơ chế **partial block decryption** thông minh.

#### Quy trình đọc chi tiết:

**Bước 1 - Nhận yêu cầu**: Terminal gửi hai tham số: offset bắt đầu và độ dài cần đọc. Hệ thống trước tiên validate yêu cầu để đảm bảo không vượt quá kích thước ảnh thực tế (sau khi loại bỏ padding).

**Bước 2 - Xác định block range**: Đây là bước quan trọng để tối ưu hiệu năng. Hệ thống tính toán block đầu tiên và block cuối cùng cần giải mã dựa trên công thức:
- `startBlock = requestedOffset / 16` (chia nguyên)
- `endBlock = (requestedOffset + requestedLength - 1) / 16`

Ví dụ: nếu terminal yêu cầu byte 40-70 (31 bytes), hệ thống sẽ xác định cần giải mã block 2, 3, và 4 (tổng 48 bytes). Mặc dù chỉ cần 31 bytes nhưng phải giải mã cả 48 bytes vì AES-CBC không cho phép giải mã một phần block.

**Bước 3 - Giải mã blocks**: Cipher được khởi tạo ở chế độ DECRYPT với Master Key và IV đã lưu từ lúc mã hóa. Sau đó, hệ thống duyệt qua từng block trong range đã xác định, giải mã tuần tự vào một temp buffer. Mỗi block mã hóa 16 bytes sẽ tạo ra 16 bytes plaintext tương ứng.

**Bước 4 - Xử lý PKCS#7 padding**: Nếu block cuối cùng trong range là block cuối của toàn bộ hình ảnh, hệ thống cần loại bỏ padding. Theo chuẩn PKCS#7, byte cuối cùng cho biết số lượng byte padding. Hệ thống đọc giá trị này (ví dụ 0x08 = 8 bytes padding) và trừ đi khỏi độ dài dữ liệu thực tế.

**Bước 5 - Trích xuất dữ liệu chính xác**: Vì hệ thống đã giải mã cả block (có thể nhiều hơn yêu cầu), bước cuối cùng là trích xuất đúng phần dữ liệu cần thiết. Offset trong temp buffer được tính bằng `requestedOffset % 16`, sau đó copy đúng số byte yêu cầu vào response buffer.

**Bước 6 - Dọn dẹp và trả về**: Master Key trong cipher được clear, temp buffer được xóa, và dữ liệu được gửi về terminal.

#### Ví dụ minh họa:

Giả sử hình ảnh gốc có 100 bytes, sau PKCS#7 padding trở thành 112 bytes (7 blocks × 16 bytes). Terminal yêu cầu đọc byte 40-70:

```
Hình ảnh gốc: 100 bytes
└─> Padding: +12 bytes (0x0C × 12) 
    └─> Encrypted: 112 bytes (7 blocks)

Yêu cầu: offset=40, length=31

Bước 1: Xác định blocks
  startBlock = 40 ÷ 16 = 2
  endBlock = (40 + 31 - 1) ÷ 16 = 70 ÷ 16 = 4
  
Bước 2: Giải mã blocks 2, 3, 4
  Block 2: bytes 32-47  (16 bytes)
  Block 3: bytes 48-63  (16 bytes) 
  Block 4: bytes 64-79  (16 bytes)
  └─> Temp buffer: 48 bytes

Bước 3: Trích xuất
  offsetInBuffer = 40 % 16 = 8
  └─> Copy tempBuffer[8:39] (31 bytes)
      = bytes 40-70 của ảnh gốc ✓
```

Cơ chế này cho phép đọc bất kỳ phần nào của hình ảnh một cách linh hoạt mà vẫn duy trì tính bảo mật của AES-CBC encryption.

## 2.7.6 Chữ ký số RSA

Bên cạnh mã hóa đối xứng AES, hệ thống còn tích hợp mã hóa bất đối xứng RSA để cung cấp khả năng xác thực thẻ thật và đảm bảo tính toàn vẹn dữ liệu thông qua chữ ký số. Đây là lớp bảo mật thứ tư trong kiến trúc phân tầng của chúng tôi.

### 2.7.6.1 Sinh và quản lý cặp khóa RSA

Khi applet được cài đặt, hệ thống cố gắng tạo một cặp khóa RSA-1024 (1024 bits). Quá trình này được thực hiện trong khối try-catch vì không phải tất cả các thẻ JavaCard đều hỗ trợ RSA - một số thẻ cũ hoặc thẻ giá rẻ có thể không có coprocessor mã hóa bất đối xứng. Nếu thẻ không hỗ trợ, hệ thống vẫn hoạt động bình thường nhưng không có chức năng chữ ký số.

Cặp khóa RSA bao gồm:
- **Private Key**: Được lưu an toàn trong persistent memory của thẻ, không bao giờ được xuất ra ngoài. Khóa này được sử dụng để tạo chữ ký số, chứng minh thẻ sở hữu identity duy nhất.
- **Public Key**: Được xuất ra terminal/server trong quá trình khởi tạo thẻ. Khóa này được lưu trữ ở phía backend để xác minh các chữ ký do thẻ tạo ra.

Quá trình sinh key pair sử dụng bộ sinh số ngẫu nhiên phần cứng của thẻ, đảm bảo tính ngẫu nhiên cao và không thể dự đoán. Với RSA-1024, private key bao gồm modulus (128 bytes) và private exponent (128 bytes), trong khi public key bao gồm cùng modulus và public exponent (thường là 3 bytes, giá trị phổ biến là 65537).

### 2.7.6.2 Xuất Public Key

Ngay sau khi cặp khóa được sinh thành công (trong lệnh `INS_INSTALL`), hệ thống tự động xuất public key về terminal. Public key được tổ chức theo cấu trúc:

```
┌────────────────────────────────┬───────────────────┐
│  Modulus (128 bytes)           │  Exponent (3 B)   │
│  n = p × q (số nguyên tố lớn)  │  e = 65537        │
└────────────────────────────────┴───────────────────┘
        Offset 0                      Offset 128
```

Terminal/server nhận public key này và lưu trữ liên kết với User ID của thẻ. Mỗi thẻ có một cặp khóa RSA duy nhất, hoạt động như "chứng chỉ số" tự ký của thẻ. Trong các hệ thống production, public key này thường được đăng ký với Certificate Authority (CA) để tạo thành một PKI (Public Key Infrastructure) hoàn chỉnh.

### 2.7.6.3 Cơ chế xác thực Challenge-Response

Chữ ký số được sử dụng chủ yếu trong giao thức xác thực challenge-response, đảm bảo thẻ đang giao tiếp là thẻ thật chứ không phải thẻ giả mạo. Quy trình hoạt động như sau:

**Bước 1 - Terminal tạo challenge**: Terminal sinh một chuỗi byte ngẫu nhiên (thường 16-32 bytes) làm challenge. Challenge này là duy nhất cho mỗi lần xác thực, ngăn chặn tấn công replay.

**Bước 2 - Gửi challenge đến thẻ**: Challenge được gửi qua lệnh `INS_SIGN_CHALLENGE`. Thẻ nhận challenge và chuẩn bị ký.

**Bước 3 - Thẻ ký challenge**: Hệ thống sử dụng thuật toán RSA-SHA-PKCS1 để ký. Quá trình bao gồm: (a) Hash challenge bằng SHA-1, tạo ra 20 bytes digest; (b) Áp dụng PKCS#1 v1.5 padding theo cấu trúc: `0x00 || 0x01 || PS || 0x00 || DigestInfo || Hash`, trong đó PS là chuỗi padding gồm các byte 0xFF để đủ 128 bytes; (c) Mã hóa cấu trúc đã padding bằng private key thông qua phép toán modular exponentiation.

**Bước 4 - Trả về chữ ký**: Chữ ký số (128 bytes cho RSA-1024) được gửi về terminal.

**Bước 5 - Terminal xác minh**: Terminal sử dụng public key đã lưu để giải mã chữ ký, trích xuất hash, hash lại challenge gốc, và so sánh hai hash. Nếu khớp, chứng tỏ thẻ sở hữu private key tương ứng và là thẻ chính hãng.

### 2.7.6.4 Ứng dụng và bảo mật

Cơ chế chữ ký số RSA cung cấp ba tính chất bảo mật quan trọng:

**Authentication (Xác thực)**: Chỉ thẻ sở hữu private key mới có thể tạo ra chữ ký hợp lệ, chứng minh identity của thẻ.

**Non-repudiation (Không thể chối bỏ)**: Một khi thẻ đã ký một giao dịch hoặc challenge, không thể phủ nhận vì chỉ private key của thẻ mới tạo ra được chữ ký đó.

**Integrity (Toàn vẹn)**: Bất kỳ thay đổi nào đối với dữ liệu đã ký sẽ làm chữ ký không hợp lệ, đảm bảo dữ liệu không bị chỉnh sửa.

Trong triển khai thực tế, chữ ký số có thể được sử dụng để ký các giao dịch quan trọng (thanh toán lớn, chuyển xu), đảm bảo terminal có thể chứng minh giao dịch thực sự được thực hiện bởi thẻ chính hãng.

## 2.7.7 Quản lý bộ nhớ bảo mật

Quản lý bộ nhớ đúng cách là yếu tố then chốt để đảm bảo dữ liệu nhạy cảm không bị rò rỉ hoặc tồn tại lâu hơn cần thiết trong bộ nhớ. JavaCard cung cấp hai loại bộ nhớ với đặc tính khác nhau, và chúng tôi sử dụng cả hai một cách chiến lược.

### 2.7.7.1 Bộ nhớ tạm thời (Transient Memory)

Transient memory là vùng RAM được tự động xóa sạch khi xảy ra các sự kiện nhất định. Hệ thống sử dụng hai chế độ transient memory:

**CLEAR_ON_RESET**: Vùng nhớ này được xóa khi thẻ bị reset (mất nguồn điện, reset vật lý). Chúng tôi lưu Master Key trong loại memory này vì sau mỗi lần reset, người dùng cần xác thực lại PIN để unwrap Master Key. Việc tự động xóa Master Key khi reset đảm bảo rằng nếu thẻ bị rút ra khỏi terminal, toàn bộ khóa giải mã dữ liệu sẽ biến mất ngay lập tức, ngăn chặn truy cập trái phép.

**CLEAR_ON_DESELECT**: Vùng nhớ này được xóa khi applet bị deselect (người dùng chuyển sang applet khác trên cùng một thẻ). Các buffer tạm thời dùng cho xử lý dữ liệu (temp buffer, chunk buffer) được lưu trong loại memory này. Điều này đảm bảo không có "dấu vết" dữ liệu nào còn sót lại khi chuyển đổi giữa các ứng dụng.

Lợi ích chính của transient memory là tính tự động - developer không cần nhớ phải xóa thủ công, hệ thống đảm bảo dữ liệu được xóa khi cần thiết. Tuy nhiên, vì nằm trong RAM, transient memory có dung lượng rất hạn chế (thường < 4KB), do đó chỉ dùng cho dữ liệu tạm thời quan trọng.

### 2.7.7.2 Bộ nhớ vĩnh viễn (Persistent Memory)

Persistent memory là EEPROM hoặc Flash memory, giữ dữ liệu ngay cả khi mất nguồn. Tất cả dữ liệu cần lưu trữ lâu dài được đặt trong loại memory này:

**Dữ liệu định danh và cấu hình**:
- `userID` (16 bytes): Định danh duy nhất của người dùng
- `salt` (16 bytes): Salt cho PBKDF2, được sinh ngẫu nhiên khi khởi tạo
- `imageType` (1 byte): Loại hình ảnh (JPG, PNG...)

**Dữ liệu mã hóa và bảo mật**:
- `wrappedMasterKey` (32 bytes): Master Key được bảo vệ bởi User PIN
- `adminWrappedMasterKey` (32 bytes): Master Key được bảo vệ bởi Admin PIN
- `masterKeyHash` (20 bytes): SHA-1 hash của Master Key để xác minh
- `imageIV` (16 bytes): IV cho mã hóa hình ảnh

**Dữ liệu người dùng được mã hóa**:
- `encryptedUserData` (256 bytes): Dữ liệu TLV đã mã hóa (tên, tuổi, xu, games...)
- `imageBuffer` (32KB): Hình ảnh đã mã hóa

**Dữ liệu trạng thái và quản lý**:
- `pinTryCounter`, `adminPinTryCounter`: Số lần thử PIN còn lại
- `lockedFlag`, `adminLockedFlag`: Cờ khóa
- `initialized`, `sessionAuth`: Cờ trạng thái
- `actualImageSize`, `encryptedImageSize`: Kích thước hình ảnh

Điều quan trọng là tất cả dữ liệu nhạy cảm trong persistent memory đều được mã hóa hoặc hash - không có plain text password, key, hoặc dữ liệu cá nhân nào được lưu trực tiếp.

### 2.7.7.3 Nguyên tắc xóa dữ liệu nhạy cảm

Một trong những lỗ hổng bảo mật phổ biến nhất là để lại dữ liệu nhạy cảm trong bộ nhớ sau khi sử dụng. Hệ thống của chúng tôi tuân thủ nghiêm ngặt nguyên tắc **"clear after use"** - xóa ngay sau khi dùng:

**Xóa PIN**: Mỗi khi PIN được nhận từ APDU, nó được copy vào một array tạm. Ngay sau khi sử dụng PIN để tạo KEK (thông qua PBKDF2), toàn bộ array này được ghi đè bằng byte 0x00. Điều này đảm bảo PIN không tồn tại trong RAM quá vài millisecond cần thiết.

**Xóa KEK**: Tương tự PIN, KEK (Key Encryption Key) chỉ tồn tại trong thời gian ngắn để unwrap hoặc wrap Master Key. Ngay sau khi hoàn tất thao tác, KEK array được xóa hoàn toàn.

**Xóa AES Key Objects**: JavaCard API cung cấp phương thức `clearKey()` cho các key object. Sau mỗi lần sử dụng temporary AES key (ví dụ cho wrapping/unwrapping), chúng tôi gọi `clearKey()` để xóa key material khỏi object, sau đó object sẽ được garbage collector thu hồi.

**Xóa temp buffers**: Các buffer tạm dùng cho mã hóa/giải mã chunk được xóa sau khi hoàn tất streaming operation, đảm bảo không có fragment dữ liệu ảnh nào còn sót lại.

Quy trình này tuân theo các best practice bảo mật quốc tế, đặc biệt là NIST SP 800-57 (Recommendation for Key Management) và PCI DSS (Payment Card Industry Data Security Standard), mặc dù đây không phải là ứng dụng thanh toán.

## 2.7.8 Chức năng quản trị và khôi phục

### 2.7.8.1 Mở khóa thẻ bằng Admin PIN

Một trong những tính năng quan trọng của hệ thống hai cấp PIN là khả năng khôi phục quyền truy cập khi người dùng quên PIN hoặc nhập sai quá số lần cho phép. Chức năng unlock được thiết kế dành riêng cho admin và yêu cầu xác thực bằng Admin PIN trước khi thực hiện.

**Quy trình mở khóa**:

Khi thẻ bị khóa (flag `lockedFlag = true`) do người dùng nhập sai PIN 3 lần, admin có thể thực hiện thao tác unlock thông qua lệnh `INS_UNLOCK_BY_ADMIN`. Đầu tiên, hệ thống xác minh admin đã xác thực thành công trong phiên hiện tại (kiểm tra `adminSessionAuth`). Nếu chưa, lệnh sẽ bị từ chối với mã lỗi yêu cầu xác thực.

Sau khi xác nhận quyền admin, hệ thống thực hiện hai thao tác chính:
1. **Reset bộ đếm**: `pinTryCounter` được khôi phục về giá trị ban đầu (3 lần)
2. **Gỡ cờ khóa**: `lockedFlag` được set về `false`, cho phép người dùng thử PIN lại

**Tùy chọn đặt lại PIN**: Ngoài việc mở khóa, admin có thể đồng thời thiết lập PIN mới cho người dùng trong cùng một lệnh. Điều này hữu ích khi người dùng hoàn toàn quên PIN cũ. Quy trình bao gồm: nhận PIN mới từ APDU data, sử dụng PBKDF2 để tạo KEK mới từ PIN mới và salt hiện tại, wrap lại Master Key bằng KEK mới, và lưu `wrappedMasterKey` mới. Quan trọng là Master Key bản thân không thay đổi, chỉ có cách nó được bảo vệ là thay đổi, do đó tất cả dữ liệu đã mã hóa vẫn có thể giải mã bình thường.

Sau khi hoàn tất, tất cả dữ liệu nhạy cảm tạm thời (new PIN, KEK) đều được xóa sạch khỏi bộ nhớ.

### 2.7.8.2 Reset toàn bộ thẻ về trạng thái ban đầu

Chức năng reset card là thao tác "nguy hiểm" nhất, xóa hoàn toàn mọi dữ liệu trên thẻ và đưa thẻ về trạng thái như mới cài đặt. Do tính nghiêm trọng của thao tác này, nó chỉ có thể được thực hiện bởi admin đã xác thực.

**Quy trình reset toàn diện**:

Khi lệnh `INS_RESET_CARD` được gọi, hệ thống thực hiện một chuỗi các thao tác xóa dữ liệu theo thứ tự:

**Bước 1 - Xóa dữ liệu định danh**: User ID, salt, và tất cả các biến cấu hình được ghi đè bằng byte 0x00.

**Bước 2 - Xóa dữ liệu mã hóa**: Wrapped Master Key (cả user và admin version), Master Key hash, encrypted user data, và encrypted image buffer đều được xóa hoàn toàn. Điều này đảm bảo không có cách nào khôi phục lại dữ liệu cũ, ngay cả khi có công cụ forensics chuyên dụng.

**Bước 3 - Xóa Master Key trong RAM**: Mặc dù transient memory tự động xóa khi reset, chúng tôi vẫn chủ động xóa Master Key để đảm bảo tuyệt đối không còn key material nào trong bộ nhớ.

**Bước 4 - Xóa RSA keys**: Nếu thẻ có hỗ trợ RSA, cả private và public key đều được clear thông qua API `clearKey()`. Điều này quan trọng vì RSA key pair là unique cho mỗi lần install, reset sẽ tạo cặp key mới hoàn toàn.

**Bước 5 - Reset tất cả flags và counters**: Các biến trạng thái được đặt về giá trị mặc định:
- `initialized = false`: Thẻ cần được khởi tạo lại
- `sessionAuth = false`, `adminSessionAuth = false`: Không còn phiên xác thực nào
- PIN counters được reset về 3
- Lock flags được gỡ bỏ

Sau khi reset hoàn tất, thẻ ở trạng thái "uninitialized" giống hệt lúc mới cài đặt applet. Người dùng phải thực hiện lại toàn bộ quy trình `INS_INSTALL` để thiết lập PIN mới, tạo Master Key mới, và đăng ký thông tin cá nhân.

**Cảnh báo bảo mật**: Vì reset xóa vĩnh viễn tất cả dữ liệu mà không có cách khôi phục, trong hệ thống production thường yêu cầu thêm các biện pháp xác nhận như: yêu cầu nhập Admin PIN hai lần, hiển thị cảnh báo rõ ràng trên terminal, hoặc thậm chí yêu cầu xác nhận qua kênh thứ hai (SMS, email) trước khi thực hiện reset.

## 2.7.9 Tổng kết và đánh giá

### 2.7.9.1 Điểm mạnh

1. **Mô hình Master Key**: 
   - Tách biệt PIN và khóa mã hóa
   - Cho phép đổi PIN mà không cần mã hóa lại dữ liệu
   - Master key được sinh ngẫu nhiên, độc lập với PIN

2. **Key Derivation mạnh mẽ**:
   - PBKDF2 với 500 iterations
   - Salt ngẫu nhiên unique cho mỗi thẻ
   - Fallback KDF khi PBKDF2 không khả dụng

3. **Mã hóa AES-CBC**:
   - IV ngẫu nhiên cho mỗi lần mã hóa
   - PKCS#7 padding chuẩn
   - Hỗ trợ streaming cho dữ liệu lớn

4. **Quản lý bộ nhớ**:
   - Transient memory cho dữ liệu nhạy cảm
   - Xóa dữ liệu ngay sau khi sử dụng
   - Persistent storage được mã hóa

5. **Authentication hierarchy**:
   - PIN người dùng cho thao tác thường xuyên
   - Admin PIN cho recovery
   - Retry counter chống brute-force

### 2.7.9.2 Khuyến nghị cải thiện

1. **Tăng PBKDF2 iterations**: 
   - Hiện tại: 500 iterations
   - Khuyến nghị: 10,000+ iterations (nếu card hỗ trợ)

2. **Sử dụng AES-256**:
   - Hiện tại: AES-128 (16 bytes)
   - Nâng cấp: AES-256 (32 bytes) cho bảo mật cao hơn

3. **Thêm Authenticated Encryption**:
   - Sử dụng AES-GCM thay vì AES-CBC
   - Đảm bảo cả confidentiality và authenticity

4. **Audit logging**:
   - Log các lần xác thực thất bại
   - Track số lần thử PIN sai

5. **Secure channel**:
   - Triển khai SCP (Secure Channel Protocol)
   - Mã hóa toàn bộ communication với terminal

### 2.7.9.3 Tuân thủ chuẩn

Hệ thống tuân thủ các chuẩn:
- **ISO/IEC 7816**: Smartcard standard
- **ISO/IEC 9797**: MAC algorithms
- **PKCS#1**: RSA cryptography standard
- **PKCS#7**: Padding scheme
- **FIPS 197**: AES encryption standard
- **RFC 2898**: PBKDF2 specification

### 2.7.9.4 Kết luận

Hệ thống mã hóa và xác thực của thẻ Entertainment triển khai một kiến trúc bảo mật đa lớp, kết hợp các thuật toán mã hóa chuẩn quốc tế với các thực hành tốt nhất trong lĩnh vực thẻ thông minh. Việc sử dụng Master Key kết hợp với PBKDF2 và AES encryption cung cấp một nền tảng bảo mật vững chắc, đồng thời cho phép linh hoạt trong quản lý PIN và dữ liệu người dùng.
